---
import Layout from '../components/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---
<Layout title="Kontakt — Lara Kutsch">
  <Header active="contact" />

  <main>
    <div class="page-bar">
      <span class="kicker">
        <span data-lang="de">Kontakt</span>
        <span data-lang="en">Contact</span>
      </span>
    </div>

    <div class="grid">
      <div class="left">
        <h1 class="headline">
          <span data-lang="de">Themenidee,<br/><em>Zusammenarbeit,</em><br/>Austausch.</span>
          <span data-lang="en">Story idea,<br/><em>collaboration,</em><br/>exchange.</span>
        </h1>

        <p class="intro">
          <span data-lang="de">Schreib mir gerne. Ich antworte innerhalb von 24 Stunden, meistens schneller. Allgemeine Anfragen, Interview-Wünsche, Themenvorschläge: alles willkommen.</span>
          <span data-lang="en">Feel free to write. I reply within 24 hours, usually sooner. General enquiries, interview requests, story pitches: all welcome.</span>
        </p>

        <div class="channels">
          <a href="mailto:larakutsch@live.de" class="mail-row" data-cursor="mail" data-cursor-label="MAIL">
            <span class="mail-addr">larakutsch@live.de</span>
            <span class="mail-label">
              <span data-lang="de">E-Mail</span>
              <span data-lang="en">Email</span>
              →
            </span>
          </a>
          <div class="social-row">
            <a href="https://www.instagram.com/lara_kutsch/" target="_blank" rel="noopener" class="social-link">
              Instagram ↗
            </a>
            <a href="https://www.linkedin.com/in/lara-kutsch-93151a1b9/" target="_blank" rel="noopener" class="social-link">
              LinkedIn ↗
            </a>
          </div>
        </div>
      </div>

      <div class="mailto-panel">
        <span class="panel-kicker">
          <span data-lang="de">Direkt schreiben</span>
          <span data-lang="en">Write directly</span>
        </span>
        <p class="panel-text">
          <span data-lang="de">Klick auf den Button – dein E-Mail-Programm öffnet sich mit meiner Adresse bereits eingetragen.</span>
          <span data-lang="en">Click the button – your email client opens with my address already filled in.</span>
        </p>
        <a href="mailto:larakutsch@live.de" class="mailto-btn" data-cursor="mail" data-cursor-label="MAIL">
          <span data-lang="de">E-Mail schreiben →</span>
          <span data-lang="en">Write an email →</span>
        </a>
      </div>
    </div>
  </main>

  <Footer />
</Layout>

<style>
  main { padding: 40px 40px 0; }

  .page-bar {
    border-bottom: 1px solid var(--ink); padding-bottom: 14px; margin-bottom: 48px;
  }
  .kicker {
    font-family: var(--font-ui); font-size: 11px; letter-spacing: 0.16em;
    text-transform: uppercase; font-weight: 600; color: var(--ink);
  }

  .grid {
    display: grid; grid-template-columns: 1.2fr 1fr;
    gap: 80px; align-items: start; margin-bottom: 96px;
  }

  .headline {
    font-family: var(--font-display); font-size: clamp(2rem, 5.5vw, 5rem);
    font-weight: 700; letter-spacing: -0.04em; line-height: 1.0; margin: 0 0 32px;
  }
  .headline em { font-style: italic; color: var(--accent); }

  .intro {
    font-family: var(--font-body); font-size: 18px; line-height: 1.6;
    max-width: 52ch; margin: 0 0 48px;
  }

  .mail-row {
    display: flex; justify-content: space-between; align-items: center;
    border-top: 1px solid var(--ink); border-bottom: 1px solid var(--ink);
    padding: 20px 0; text-decoration: none; color: var(--ink); margin-bottom: 18px;
  }
  .mail-addr {
    font-family: var(--font-display); font-size: clamp(1.25rem, 2.5vw, 2rem);
    font-weight: 700; letter-spacing: -0.025em;
  }
  .mail-label {
    font-family: var(--font-ui); font-size: 11px;
    letter-spacing: 0.16em; text-transform: uppercase; color: var(--fg-2);
    white-space: nowrap;
  }

  .social-row { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
  .social-link {
    border-bottom: 1px solid var(--hairline); padding: 14px 0;
    text-decoration: none; color: var(--ink);
    font-family: var(--font-body); font-size: 16px;
  }

  /* Mailto panel */
  .mailto-panel {
    padding: 40px 36px;
    border: 2px solid var(--ink); background: var(--bg-2);
    display: grid; gap: 24px; align-content: start;
  }
  .panel-kicker {
    font-family: var(--font-ui); font-size: 11px; letter-spacing: 0.16em;
    text-transform: uppercase; font-weight: 600; color: var(--ink);
  }
  .panel-text {
    font-family: var(--font-body); font-size: 16px; line-height: 1.55;
    color: var(--fg-2); margin: 0; max-width: 38ch;
  }
  .mailto-btn {
    display: inline-block; align-self: start;
    background: var(--accent); color: var(--accent-ink); text-decoration: none;
    padding: 16px 24px; font-family: var(--font-ui); font-size: 12px;
    letter-spacing: 0.16em; text-transform: uppercase; font-weight: 700;
  }
  .mailto-btn:hover { color: var(--accent-ink); }

  @media (max-width: 900px) {
    .grid { grid-template-columns: 1fr; gap: 40px; }
  }
  @media (max-width: 600px) {
    main { padding: 20px 20px 0; }
  }
</style>
